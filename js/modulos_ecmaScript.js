//Modulos de ecmaScript
//Se usan mucho en web y react
import multiplikeichon, {
  sumar,
  restar as restarNumeros,
} from "./modulos_ejemplo.js";
//modulos es el import jaja

const resultado = sumar(10, 20);
console.log(resultado);

const resultado2 = restarNumeros(30, 10);
console.log(resultado2);

const resultado3 = multiplikeichon(10, 20);
console.log(resultado3);
