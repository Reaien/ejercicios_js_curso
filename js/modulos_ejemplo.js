//export default modules

export const sumar = (n1, n2) => {
  return n1 + n2;
};

export const restar = (n1, n2) => {
  return n1 - n2;
};

//export default modules
//Solo puede existir 1 export default por archivo pero puedes nombrarlo como tu quieras

const multiplicar = (n1, n2) => {
  return n1 * n2;
};

export default multiplicar;
